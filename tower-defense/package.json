{
  "name": "cursor-crew-tower-defense",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Cursor Crew — Defrag Run (Three.js arena / tower-defense prototype).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm test && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/jsdom": "^28.0.1",
    "@types/three": "^0.184.0",
    "jsdom": "^29.0.2",
    "typescript": "^5.6.3",
    "vite": "^6.0.5",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
